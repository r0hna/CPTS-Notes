# Sensitive data exposure
Sometime the data (secrets) are exposed at client-side (Website source code)
- check for sensitive *data/comments* in source code
- Check for sensitive in formation in `.js` files